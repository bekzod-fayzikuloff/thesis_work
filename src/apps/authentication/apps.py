from django.apps import AppConfig


class AuthenticationConfig(AppConfig):
    """apps.authentication configuration define"""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.authentication"
